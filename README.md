# Implement remote control with Python

bectrl is the code of the controlled end

ctrl is the control code

socks5 proxy is recommended to apply for one at novpn.cn

# install dependencies

`pip install -r requirements.txt`
